[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "travel-mate-2"
version = "0.1.0"
description = "Travel expense data model and services"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
